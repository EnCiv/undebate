'use strict'

// for code that runs on the client side and includes models, it should instead include this file (using webpack) so that mungo and mongo and etc are not pulled in on the client side
export default class model {}
